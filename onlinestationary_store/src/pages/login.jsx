import Base from "../components/Base";

const login=()=>{
    return(
        <Base
        title="StationeryStore / Login"
        description="Get logged in to get access to the store and can view the cart details and view your user info!"
        >
            <h1>This is login page</h1>
        </Base>
    );
}

export default login;